{
  "name": "mjml-starter",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wait-on ./index.html && npx http-server ./ -p 3000 -o"
  },
  "dependencies": {
    "http-server": "^14.1.1",
    "mjml": "^4.15.3",
    "mjml-server": "^1.0.5"
  },
  "devDependencies": {
    "wait-on": "^8.0.2"
  }
}
