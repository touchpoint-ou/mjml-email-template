{
  "name": "mjml-starter",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && wait-on ./index.html && browser-sync start --server ./ --files './index.html' --port 3000"
  },
  "dependencies": {
    "mjml": "^4.15.3"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "http-server": "^14.1.1",
    "wait-on": "^8.0.2"
  }
}
